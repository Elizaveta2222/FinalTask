@model BusinessLogic.Models.LecGroup;
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Группа</h1>
    @using (Html.BeginForm("CreateLecGroup", "Home", FormMethod.Post))
{
    @Html.HiddenFor(m => m.Id)
    <div class="row g-3 align-items-center p-4">
        <div class="col-3"></div>
            <div class="col-2">
                <label for="inputName" class="col-form-label">Номер</label>
            </div>
            <div class="col-3">
                @Html.TextBoxFor(m => m.Number, new { @class = "form-control"})
            </div>
    </div>
    <button type="submit" class="btn btn-primary">Сохранить</button>
    }

</div>